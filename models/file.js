/*
const mongoose = require('mongoose');

const scheme = mongoose.Schema({
    filename: {
        type: String,
        required: true,
    },
    dateCreated: {
        type: Date,
        required: true,
    },
    size: {
        type: Number
    },
    hasWatched: {
        type: Boolean
    },
});
*/